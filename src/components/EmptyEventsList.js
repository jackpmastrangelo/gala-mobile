import React from 'react';
import { Text } from 'react-native';

export default class EmptyEventsList extends React.Component {
    render() {
        return (
            <Text>No events! You should make one!</Text>
        );
    }
}